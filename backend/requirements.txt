# Versions are left open so pip resolves builds that ship wheels for your
# Python version. Verified working on Python 3.14 (Windows).
fastapi>=0.115
uvicorn[standard]>=0.34
# [email] extra: routes/auth.py uses EmailStr, which needs email-validator
# at runtime. It is never imported directly, so a source scan will not find it.
pydantic[email]>=2.11
pydantic-settings>=2.7
SQLAlchemy>=2.0.36
# psycopg v3 (not psycopg2) -- psycopg2-binary has no wheel for Python 3.13+.
psycopg[binary]>=3.2
httpx>=0.28
python-dotenv>=1.0
python-multipart>=0.0.20
pytest>=8.3

# --- Core optimization engine. The project does not run without this. ---
# Verified working: 9.15.6755
ortools>=9.15

# --- Authentication ---
# Verified working: bcrypt 5.0.0, PyJWT 2.14.0
bcrypt>=4.2
PyJWT>=2.9

# --- AI explanation layer (optional at runtime, required to import) ---
# Verified working: 2.24.0
google-genai>=2.0
